process {
    withName: FASTP {
        publishDir = [
            path: { "${params.outdir}/fastp/${meta.id}" },
            enabled: false
        ]
    }

    withName: HUMANN {
        publishDir = [
            path: { "${params.outdir}/humann/${meta.id}" }
        ]
    }
}